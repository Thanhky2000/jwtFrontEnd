import React from 'react';

const Login = (props) => {
    return (
        <div className='login-container'>
            login form
            <button> Test</button>
        </div>
    );
}

export default Login;